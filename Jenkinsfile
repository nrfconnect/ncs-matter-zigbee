@Library('CI_LIB@trigger_branch_plan') _

// Note: sdk_smart_home is a reserved name from the test repo and must remain as is.
def pipeline = new ncs.sdk_smart_home.Main()

pipeline.run("matter-zigbee")
